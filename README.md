# FunBets TV

TizenBrew module that opens:

`https://cas.oqyb-drtbrt-aux2.online/tv/`

Install from TizenBrew's npm module screen:

`funbets-tv`

This uses the same website-launch pattern as TFlix: `packageType: "mods"` plus `websiteURL`.
